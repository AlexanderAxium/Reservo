/** Session max age: 7 days (in seconds) */
export const SESSION_MAX_AGE = 60 * 60 * 24 * 7;

/** Session update age: 1 day (in seconds) */
export const SESSION_UPDATE_AGE = 60 * 60 * 24;

/** Reservation slot duration: 1 hour (in milliseconds) */
export const SLOT_DURATION_MS = 60 * 60 * 1000;

/** Default stale time for React Query (5 minutes in milliseconds) */
export const STALE_TIME = 5 * 60 * 1000;

/** Request timeout for password reset forms (15 seconds in milliseconds) */
export const REQUEST_TIMEOUT_MS = 15_000;
